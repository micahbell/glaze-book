module.exports = {

  recipeFinder: function(recID, recArray) {
    //loop through arrayObjectIndexOf
    //find the obejct that matches recid at index of()
  }


    var position = recipesArray.map(function(recipe) { return recipe.recipesArray; }).indexOf(recID);

  var position = user_ids.map(function(x) {return x.user_id;}).indexOf(currentUser);
     var found = user_ids[position];


function arrayObjectIndexOf(myArray, searchTerm, property) {
   for(var i = 0, len = myArray.length; i < len; i++) {
       if (myArray[i][property] === searchTerm) return i;
   }
   return -1;
}
arrayObjectIndexOf(arr, "stevie", "hello"); // 1




};
